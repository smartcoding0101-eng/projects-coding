import ServicioLayout from './Partials/ServicioLayout';

export default function Tienda({ servicio, whatsappSoporte }) {
    return <ServicioLayout servicio={servicio} whatsappSoporte={whatsappSoporte} />;
}
