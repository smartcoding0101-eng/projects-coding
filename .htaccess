<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirigir todas las peticiones a la carpeta public/ de forma silenciosa
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
